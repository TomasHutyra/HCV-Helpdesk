server {
    listen 80;
    server_name _;

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Přesměrování HTTP → HTTPS (zapněte po nastavení SSL)
    # return 301 https://$host$request_uri;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
    }

    # Přílohy tiketů — přes Django (kontrola oprávnění)
    location /media/tickets/attachments/ {
        deny all;
    }

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        client_max_body_size 50M;
    }
}

# HTTPS blok (odkomentujte po získání certifikátu)
# server {
#     listen 443 ssl;
#     server_name helpdesk.vase-domena.cz;
#
#     ssl_certificate     /etc/letsencrypt/live/helpdesk.vase-domena.cz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/helpdesk.vase-domena.cz/privkey.pem;
#
#     location /static/ { alias /app/staticfiles/; }
#     location /media/  { alias /app/media/; }
#     location /media/tickets/attachments/ { deny all; }
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-Proto https;
#     }
# }
